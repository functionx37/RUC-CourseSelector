[project]
name = "ruc-courseselector"
version = "0.1.0"
description = "Local, session-based RUC course selection CLI"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pyreadline3>=0.2; sys_platform == 'win32'",
    "websocket-client>=1.8",
]
